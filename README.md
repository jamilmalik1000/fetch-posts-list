# fetch-posts-list
A simple JavaScript project that fetches a list of posts using the Fetch API and displays all titles dynamically in the browser.
# Fetch Posts List
A beginner-friendly JavaScript project that fetches a list of posts from a public API and displays each post title in the browser using the Fetch API.

## 📌 Features
- Pure JavaScript (no frameworks)
- Fetches JSON data from API
- Displays list of titles dynamically
- Error handling using async/await

## 🚀 How to Run
1. Clone the repository:
   ```bash
   git clone https://github.com/jamilmalik1000/fetch-posts-list
Open the project folder.

Simply open index.html in the browser.

📡 API Used
Data is fetched from:

arduino
Copy code
https://jsonplaceholder.typicode.com/posts
